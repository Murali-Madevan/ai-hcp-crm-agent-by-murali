# Backend API base URL
# Leave empty to use Vite proxy (local dev) via /api/agent
# Set to deployed backend URL for production, e.g.:
# VITE_API_BASE_URL=https://your-backend.vercel.app
VITE_API_BASE_URL=
