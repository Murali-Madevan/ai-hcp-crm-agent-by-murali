# No API base URL needed — uses relative /api/* paths
# Local dev: Vite proxy in vite.config.ts forwards /api/ -> localhost:8000
# Production: Vercel rewrites /api/* -> api/index.py (FastAPI)
#
# Required env vars on Vercel:
# GROQ_API_KEY=<your-groq-api-key>
